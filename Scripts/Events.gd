extends Node

#This is a Node with Global Events

const TREE_PLANT = "tree_plant"
signal tree_plant(tree_instance)

const TREE_BURN = "tree_burn"
signal tree_burn(tree_instance)

const ITEM_SELECT = "item_select"
signal item_select(item)
